# Contracts

Required interfaces:
- `AIProvider`
- `Tool`
- `MemoryStore`
- `PermissionEngine`
- `Verifier`
- `STTProvider`
- `TTSProvider`

Tool result should be standardized and machine-readable.

Avatar protocol carries high-level:
`emotion`, `gesture`, `expression`, `action`, `speech_state`.

No direct LLM bone/frame control.
