"""CHAOS entry point (Milestone 0 — Foundation).

Minimal CLI placeholder: proves the package installs and runs.
Real agent loop, brain runtime, and tool execution belong to
later milestones and must go through validation → permission →
execution → verification. Nothing here executes LLM output.
"""


def main() -> int:
    """Print the foundation banner. Returns process exit code."""
    print("CHAOS 0.0.1 — Foundation (Milestone 0)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
